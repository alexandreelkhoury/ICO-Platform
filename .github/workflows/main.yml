name: check-node-version
run-name: check-node-version
on:
  push : 
    branches:
      - 'master'
jobs:
  check-node-version:
    runs-on: ubuntu-latest
    steps:
      - run: docker build -t alexkhoury/frontend-ico frontend
      - run: docker push alexkhoury/frontend-ico