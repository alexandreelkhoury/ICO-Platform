name: upload-to-dockerhub
run-name: upload-to-dockerhub
on:
  push : 
    branches:
      - 'master'
jobs:
  upload-to-dockerhub:
    runs-on: ubuntu-latest
    steps:
      - run: docker build -t alexkhoury/frontend-ico .
      - run: docker push alexkhoury/frontend-ico